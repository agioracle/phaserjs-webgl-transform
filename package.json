{
  "name": "minigame-phaserjs-transform-sdk",
  "private": true,
  "version": "0.1.0",
  "description": "Tool for converting Phaser.js games to WeChat Mini-Games",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "clean": "pnpm -r run clean"
  },
  "dependencies": {
    "@aspect/cli": "workspace:*"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
